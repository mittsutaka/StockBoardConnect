import React from 'react';
import { Layout } from '../../organisms/shared/Layout.jsx';

export const FriendIndex = (props) => {
    return (
        <Layout sideSelected='フレンド'>
        </Layout>
    )
}