import React from 'react';
import { Layout } from '../../organisms/shared/Layout.jsx';

export const MessageIndex = (props) => {
    return (
        <Layout sideSelected='メッセージ'>
        </Layout>
    )
}