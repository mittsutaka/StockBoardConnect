import React from 'react';
import { Layout } from '../../organisms/shared/Layout.jsx';

export const HomeIndex = (props) => {
    return (
        <Layout sideSelected='ホーム'>
        </Layout>
    )
}