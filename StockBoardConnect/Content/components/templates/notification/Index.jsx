import React from 'react';
import { Layout } from '../../organisms/shared/Layout.jsx';

export const NotificationIndex = (props) => {
    return (
        <Layout sideSelected='通知'>
        </Layout>
    )
}